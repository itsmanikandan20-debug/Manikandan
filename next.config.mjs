// Real results come from arbitrary third-party domains (search results,
// imgbb-hosted uploads), which can't be known ahead of time — so dynamic
// images use the `unoptimized` prop instead of a remotePatterns allowlist.
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
